/**
 * A collection of values representing the current API version
 * @property git - The latest git commit for this version
 * @property version - The version of the API instance
 */
export class ServerVersion {
  git: string;
  version: string;
}
