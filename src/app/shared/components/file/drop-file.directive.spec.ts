/**
 * Todo do this test
 */


import { DropFileDirective } from '@app/shared/components/file/drop-file.directive';

describe('DropFileDirective', () => {
  it('should create an instance', () => {
    // const directive = new DropFileDirective();
    // expect(directive).toBeTruthy();
  });
});
