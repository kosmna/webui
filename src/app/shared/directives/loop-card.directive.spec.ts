import { ElementRef } from '@angular/core';
/**
 * TODO do this unit test
 */
import { LoopCardDirective } from '@app/shared/directives/loop-card.directive';

describe('LoopCardDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopCardDirective();
    // expect(directive).toBeTruthy();
  });
});
