import { LoopCounterDirective } from '@app/shared/directives/loop-counter.directive';

describe('LoopCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopCounterDirective();
    expect(directive).toBeTruthy();
  });
});
