/**
 * A model class for Device Management API "device info"
 */
export class DeviceInfo {
  firmwareVersion: string;
  hardwareVersion: string;
  manufacturer: string;
  modelNumber: string;
  serialNumber: string;
}
